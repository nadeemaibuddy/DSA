#include<iostream>
usng namespace std;
int main()
{
    cout<<((5+10)*2-(5+2));
}

// | Operators               | Associativity    |
// | ----------------------- | ---------------- |
// | ( ),[]                  | **Left → Right** |
// | `++` `--` `+` `-` `!`   | **Right → Left** |
// | `*` `/` `%`             | **Left → Right** |
// | `+` `-`                 | **Left → Right** |
// | `<<` `>>`               | **Left → Right** |
// | `<` `>` `<=` `>=`       | **Left → Right** |
// | `==` `!=`               | **Left → Right** |
// | `&&`                    | **Left → Right** |
// | `                       | `                |
// | `=` `+=` `-=` `*=` `/=` | **Right → Left** |
